
# bkit runtime/session state (generated by hooks)
.bkit/

# Harness-local permissions (machine-specific)
.claude/settings.local.json

# SEO 산출물 (명령어로 재생성 가능하므로 커밋하지 않음)
data/keywords/output/
data/business.profile.json.bak

# ⚠️ data/business.profile.json, data/progress.json, data/keywords/*.csv 는
#    일부러 커밋합니다. Claude Code 웹 세션은 컨테이너가 매번 초기화되므로
#    커밋해두지 않으면 입력한 정보와 진행 상황이 사라집니다.

# 씨앗 키워드 산출물 (재생성 가능)
data/keywords/seeds/
