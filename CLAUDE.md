# nonos — 소상공인·커머스 프로그램 개발 베이스

이 저장소는 **소상공인과 커머스 사용자를 위한 프로그램**을 빠르고 반복적으로 만들기 위한 공통 기반입니다.
새 프로그램을 만들 때마다 이 문서의 규칙과 `.claude/agents/`의 전용 에이전트를 재사용합니다.

> 사용자는 비개발자일 수 있습니다. 전문 용어보다 **쉬운 설명 + 동작하는 결과물**을 우선합니다.

---

## 1. 기본 기술 스택 (추천 기본값)

특별한 요구가 없으면 아래 스택으로 만듭니다. (코딩을 최소화하고 빠르게 출시하기 위한 선택)

| 영역 | 선택 | 이유 |
|------|------|------|
| 프론트엔드 | **Next.js (App Router) + TypeScript** | 웹앱·관리자·모바일웹을 한 번에. SEO·속도 우수 |
| 스타일 | **Tailwind CSS** | 빠른 UI, 일관된 디자인 |
| 백엔드/DB | **bkend.ai (BaaS)** | 인증·DB·파일·API를 직접 서버 구축 없이. → `bkend-expert` 에이전트 활용 |
| 인증 | bkend.ai Auth (이메일/소셜) | 회원·사장님·직원 권한(RBAC) |
| 배포 | Vercel (웹) | 무료~저비용, 자동 배포 |

> 모바일 전용 앱이 필요해지면 React Native/Expo로 확장 (그때 별도 안내).

---

## 2. 도메인 모듈 (만들 프로그램의 4대 영역)

소상공인 커머스 프로그램은 대부분 아래 4개 모듈의 조합입니다. 각 모듈마다 전담 에이전트가 있습니다.

| 모듈 | 다루는 것 | 전담 에이전트 |
|------|-----------|--------------|
| 🛒 **쇼핑몰/주문** | 상품, 장바구니, 결제, 주문/배송 상태 | `commerce-shop-expert` |
| 📅 **예약/접수** | 예약 슬롯, 대기, 노쇼, 일정 캘린더 | `commerce-booking-expert` |
| 📦 **재고/POS/매출** | 재고 수량, 매장 판매(POS), 매출·정산 집계 | `commerce-inventory-pos-expert` |
| 💬 **고객/마케팅** | 단골 관리, 쿠폰·포인트, 문자/알림 발송 | `commerce-crm-expert` |
| 🇰🇷 **한국 특화(선택)** | 국내 결제(카카오·토스·네이버), 배달, 세금계산서·현금영수증 | `commerce-korea-expert` |

요구사항이 어느 모듈인지 헷갈리면 **`commerce-planner`** 에이전트가 정리하고 적절한 전담 에이전트로 연결합니다.

---

## 3. 공통 데이터 모델 (모듈이 공유하는 핵심 개념)

새 기능을 만들 때 아래 공통 엔티티를 먼저 떠올립니다. (bkend.ai 테이블로 매핑)

- **Store(매장/사업장)** — 멀티 매장 대비. 모든 데이터는 `storeId`로 구분
- **Product(상품/메뉴/서비스)** — 가격, 옵션, 재고 연결
- **Customer(고객)** — 연락처, 단골 등급, 포인트
- **Order(주문/예약)** — 상태 머신: `생성 → 결제 → 준비 → 완료/취소`
- **Payment(결제)** — 금액, 수단, 환불 상태
- **Staff(직원)** — 권한(사장/매니저/알바)

> 새 프로그램이라도 이 모델을 재사용해 일관성을 유지합니다.

---

## 4. 코딩·폴더 규칙

```
app/                # Next.js 라우트 (화면)
  (admin)/          # 사장님/직원용 관리자
  (shop)/           # 고객용 매장/주문 화면
components/          # 재사용 UI 컴포넌트
lib/                # bkend 클라이언트, 유틸, 도메인 로직
  bkend/            # bkend.ai 연동 코드
  domain/           # 주문/재고/예약 등 비즈니스 규칙
types/              # 공통 타입 (Order, Product ...)
```

- 화면 텍스트·날짜·금액은 **한국어/원화(₩) 기본**, 다국어는 나중에 확장 가능하게 분리
- 금액은 항상 **정수(원)** 로 저장 (소수점·부동소수 금지)
- 주문/결제 등 **상태 변경은 반드시 명시적 상태값**으로 (자유 텍스트 금지)

---

## 5. 에이전트 사용법 (자주 쓰는 방식)

기능을 만들 때 자연어로 요청하면 자동으로 알맞은 에이전트가 동작합니다. 예:

- "온라인 쇼핑몰에 장바구니랑 결제 붙여줘" → `commerce-shop-expert`
- "미용실 예약 받는 화면 만들어줘" → `commerce-booking-expert`
- "재고 떨어지면 알려주는 기능" → `commerce-inventory-pos-expert`
- "단골한테 쿠폰 문자 보내기" → `commerce-crm-expert`
- "카카오페이 결제, 세금계산서 발행" → `commerce-korea-expert`
- "뭐부터 만들어야 할지 모르겠어" → `commerce-planner` (전체 정리·로드맵)

보조로 bkit 기본 에이전트도 함께 씁니다: `bkend-expert`(DB/인증), `frontend-architect`(UI),
`product-manager`(요구사항), `code-analyzer`(품질 점검), `security-architect`(보안·결제).

---

## 6. 마케팅/유입 모듈 — 구글 SEO · 구글맵 상위노출

만든 프로그램에 **손님이 들어오게** 만드는 축. 코드가 아니라 운영 체계다.

| 산출물 | 위치 |
|--------|------|
| 알고리즘 분석·기획·실행 문서 8종 | `docs/seo/` ([시작점](docs/seo/README.md)) |
| 구글맵(GBP) 상위노출 세팅 실행서 | `docs/seo/05-google-maps-gbp.md` |
| 즉시 실행 체크리스트 3종 | `docs/seo/checklists/` |
| 씨앗 키워드 생성기 | `tools/seo/make-seeds.mjs` (`npm run seo:seeds`) |
| 키워드 우선순위 산출기 | `tools/seo/score-keywords.mjs` (`npm run seo:score`) |
| Next.js 기술 SEO 템플릿 | `templates/next-seo/` |
| 복합 업종·신규 등록 실행서 | `docs/seo/08-multi-vertical-gbp.md` |
| 사업장 단일 진실 원천 | `data/business.profile.json` |

원칙:
- **검색량·CPC 수치는 지어내지 않는다.** 실측 데이터(Keyword Planner/GSC)가 있을 때만 확정 우선순위표를 만든다.
- **순위를 자동으로 바꾸는 방법은 없다.** 구글이 읽는 신호를 정확히 채우는 것이 전부다.
- 상호명 키워드 삽입·대가성 리뷰·가짜 리뷰는 **프로필 정지 사유**이므로 절대 제안하지 않는다.

---

## 6. 안전 원칙

- 결제·고객정보·매출 같은 **민감 데이터**는 항상 서버(bkend) 권한 검증 후 처리. 클라이언트에서 금액 신뢰 금지.
- 자동으로 백그라운드 실행되는 코드/훅은 두지 않습니다 (사용자 요청 시에만 명시적으로 추가).
- 외부로 데이터를 보내거나 결제 연동을 켜기 전에는 반드시 사용자에게 확인.
